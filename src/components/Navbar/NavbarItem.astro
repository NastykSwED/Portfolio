---
type Props = {
	name: string;
};

const { name } = Astro.props;
---

<li class='re-Navbar-listItem'>
	<a
		class='re-Navbar-link'
		href={`#re-${name.includes('Contact') ? 'Contact' : name}Section`}
	>
		{name}
	</a>
</li>
